# SELinux configuration

SELINUX=enforcing
SELINUXTYPE=targeted

# Policy modules
POLICY_MODULES="embedded_device application"

# Context files
FILE_CONTEXTS="/etc/selinux/targeted/contexts/files/file_contexts"

